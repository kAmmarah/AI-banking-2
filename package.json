{
  "name": "ai-banking-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd ai-fraud-detection/frontend && npm install && cd ../backend && npm install",
    "build": "CI=false cd ai-fraud-detection/frontend && npm run build"
  },
  "workspaces": [
    "ai-fraud-detection/frontend",
    "ai-fraud-detection/backend"
  ],
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@google/generative-ai": "0.24.1"
  }
}
